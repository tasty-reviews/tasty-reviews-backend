spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/reviews_db
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jwt:
    secret: dljfjaldkfjaksldfjaldkfjlkadsfjakslfjafejfopajdiofjaiodfdlksfjakldfjakldsf
  config:
    import: classpath:application-mail.yml
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB


  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
server:
  servlet:
    session:
      cookie:
        http-only: false

naver:
  client:
    id: z5_1N93ZPYttDAeegf1v
    secret: Y7N1zcMl1N

logging:
  level:
    root: INFO # 전체 로그 레벨 설정
    org:
      springframework:
        security: DEBUG # Spring Security 로그 레벨 설정

      hibernate:
        SQL: DEBUG # Hibernate SQL 로그 레벨 설정

file:
  upload-dir: C:\pictures